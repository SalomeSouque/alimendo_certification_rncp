# URL de base du backend FastAPI.
VITE_API_URL=http://localhost:8000

# Mode d'authentification.
#   true (ou absent) = auth MOCKÉE (aucun backend requis).
#   false            = vrai backend (POST /auth/login + GET /auth/me).
VITE_AUTH_MOCK=true
